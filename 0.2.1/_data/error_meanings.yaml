# Hand-curated SaldeoSMART error-code catalog.
#
# This file is the authoritative source for the "SaldeoSMART-issued codes"
# table on docs/reference/error-codes.md (auto-generated by
# scripts/gen_error_codes.py). Add entries here as you discover new codes.
#
# Schema:
#   codes:
#     "<code>":
#       meaning: One-sentence explanation.
#       recovery: One-sentence action — what fixes the failure.

codes:
  "4401":
    meaning: No SEARCH_POLICY found in file.
    recovery: The `document/search` request body is missing `<SEARCH_POLICY>` or has the wrong tag (`<POLICY>`). Confirmed via the `_build_search_xml` builder — fix the input shape.
